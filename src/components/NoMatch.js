import React from 'react';
import * as styles from './NoMatch.scss';

const NoMatch = () => (
  <div className={styles.noMatch}>
    <h1>Oops! It seems you broke the system...</h1>
  </div>
);

export default NoMatch;
